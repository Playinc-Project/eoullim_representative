# AWS RDS 정보 (RDS 엔드포인트로 변경)
DATABASE_HOST=your-rds-endpoint.amazonaws.com
DATABASE_PORT=3306
DATABASE_USER=eoullim
DATABASE_PASSWORD=your_strong_password
DATABASE_NAME=eoullim
DATABASE_SYNCHRONIZE=false

# Server (운영 환경)
PORT=8081
NODE_ENV=production

# Logging
LOG_LEVEL=info

# Timezone (Spring과 동일)
TZ=Asia/Seoul

# Database Sync (운영 false)
DATABASE_SYNCHRONIZE=false
